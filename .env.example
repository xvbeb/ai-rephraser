GEMINI_API_KEY=
AI_MODEL=gemini-2.5-flash
AI_TIMEOUT_MS=30000
# Generate with: python -c "import secrets; print(secrets.token_hex(32))"
# Required for production. Empty in development = random key per process.
SECRET_KEY=
APP_ENV=development
HOST=127.0.0.1
PORT=5000
FLASK_DEBUG=false
